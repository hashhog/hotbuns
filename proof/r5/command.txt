# Re-run R5 (operator RPC subset). Needs the hashhog meta-repo instruments
# and a running node (live fleet or the regtest harness).
#
# Live lane (T1/T2; T3 is SKIP-REGTEST here). The mainnet unit is currently
# maintenance-paused; this will connection-refuse until it is started:
#   python3 tools/r5_probe.py --impl hotbuns -v
#
# Regtest lane (T3 wallet + stop/getblockfilter):
#   REGTEST_SLOTS=1 REGTEST_MEM=8G bash tools/regtest-slot.sh \
#     bash tools/regtest-harness.sh --r5-lane=hotbuns
#
# In-repo control re-run by proof/verify.sh (no live node required):
#   HOTBUNS_UNIT_CHILD=1 bun test ./src/__tests__/bip66_height_gate.test.ts ./src/script/interpreter.test.ts
